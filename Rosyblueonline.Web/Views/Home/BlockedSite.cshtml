
@{
    ViewBag.Title = "BlockedSite";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<!doctype html>
<html>
<head>
    <title>Down for maintenance</title>

    <style type="text/css">
        
        img {
            max-width: 100%
        }

        .content {
            background: #ffffff;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-shadow: 0 1px 0 rgba(0,0,0,0.2);
            margin: 60px auto 0;
            padding: 20px;
            width: 640px;
        }
    </style>
</head>
<body>



    <div class="content">


        <p> <img src="/Content/img/page-underConstructionblocked.png" />		</p>

    </div>
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
    <!-- This comment is just to take the page size above 512 bytes, as IIS doesn't pick the file up properly otherwise. -->
</body>
</html>